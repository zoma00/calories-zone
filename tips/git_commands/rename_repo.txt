I’m going to do two things now: create the rename commit you approved, and verify/update your `origin` remote URL. Then I’ll tell you exactly what still needs to be done on GitHub side for an actual repo rename.

Ran terminal command: Set-Location '/home/hazem-elbatawy/Downloads/zomba_cal'; git status --short --branch

Ran terminal command: Set-Location '/home/hazem-elbatawy/Downloads/zomba_cal'; git remote -v

Your remote is already set to that URL, so I’ll proceed with the commit and push now. After that, I’ll explain how to rename the GitHub repository itself to match “calories-zone.”

Ran terminal command: Set-Location '/home/hazem-elbatawy/Downloads/zomba_cal'; git add -A; git commit -m 'Rename app to Calories Zone and migrate package to com.calories.zone'; git push

Done.

- Commit created and pushed:
  46da289
- Branch pushed:
  main -> origin/main
- Remote currently in use:
  git@github.com:zoma00/zomba-cal.git

Important point:
That URL is still the old repo name (zomba-cal). I cannot rename the GitHub repository itself from here.

To rename the repo on GitHub to calories-zone:
1. Open your repo on GitHub.
2. Go to Settings -> General -> Repository name.
3. Change from zomba-cal to calories-zone.
4. Save.

After you rename on GitHub, run this locally:
git remote set-url origin git@github.com:zoma00/calories-zone.git
git remote -v

If you want, I can run the remote update command for you right after you rename it on GitHub.


hazem-elbatawy@hazem-elbatawy-Vostro-15-3510:~/Downloads/zomba_cal$ git remote -v
origin  git@github.com:zoma00/calories-zone.git (fetch)
origin  git@github.com:zoma00/calories-zone.git (push)